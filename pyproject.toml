[project]
name = "deployer"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Rocky Chen", email = ""},
]
dependencies = ["pyyaml>=6.0.3"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
